package capgemini_29012026;

public class stringbuffer_stringbuilder {

	public static void main(String[] args) {
		// StringBuffer and StringBuilder in Java:
		// Both StringBuffer and StringBuilder are classes in Java that are used to create mutable (modifiable) sequences of characters.
		// The main difference between them is that StringBuffer is synchronized (thread-safe), while StringBuilder is not synchronized (not thread-safe).
		// 
		// String vs StringBuffer vs StringBuilder:
		// String: Immutable (cannot be changed after creation)
		// StringBuffer: Mutable and synchronized (thread-safe)
		// StringBuilder: Mutable and not synchronized (not thread-safe)
		
		// 

	}

}
