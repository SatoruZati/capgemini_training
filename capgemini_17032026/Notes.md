Main class of a Spring Boot application is called Configuration class. It is the entry point of the application and is responsible for bootstrapping the Spring Boot application context. 

The Configuration class is typically annotated with `@SpringBootApplication`, which is a convenience annotation that combines `@Configuration`, `@EnableAutoConfiguration`, and `@ComponentScan` annotations.